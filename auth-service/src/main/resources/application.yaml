server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/authdb?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: logitrack-secret-key-2024-muito-segura
  expiration: 86400000

management:
  endpoints:
    web:
      exposure:
        include: health, metrics